name: Terraform Deployment to Azure

on:
  push:
    branches:
      - main  # or your chosen branch
  pull_request:
    branches:
      - main

jobs:
  terraform:
    name: 'Terraform CI/CD'
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout Code
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Step 2: Login to Azure
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Step 3: Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 4.36.0

      # Step 4: Terraform Init
      - name: Terraform Init
        run: terraform init

      # Step 5: Terraform Plan (Check changes)
      - name: Terraform Plan
        run: terraform plan

      # Step 6: Terraform Apply (Real deployment)
      # Note: In real scenario, you might restrict this to specific conditions or environments
      - name: Terraform Apply (Placeholder)
        if: github.ref == 'refs/heads/main'
        run: |
          echo "This would normally run: terraform apply -auto-approve"
          # terraform apply -auto-approve
      # Step 7: Output Result (Simulated Success/Failure)
      - name: Deployment Result Placeholder
        run: |
          echo "Deployment Succeeded ✅"
          # In a real-world setup, check status and handle errors accordingly
